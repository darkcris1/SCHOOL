
# FreeCodeGram with Docker

### Install docker

- Open the docker desktop
- run ```bash docker-compose up --build```


- Go to Docker Desktop laravel-1 container and run ```php artisan migrate``` in terminal